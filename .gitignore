# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/
.claude/

# Binary
/delve-helper

# Runtime artifacts (generated debug output)
# Ensure full examples/ is committed (templates, failing_test, crash). .debug_*/ below still ignores artifact dirs under examples.
!/examples/
!/examples/**
.debug_*/
.dlv/
debug_report.pdf
debug_report_*.pdf
# Allow example PDF reports to be committed (linked from README)
!examples/failing_test/debug_report_*.pdf
!examples/crash/debug_report_*.pdf

# Generated skills (make generate-skills); only skills/source/ is committed
skills/claude/
skills/codex/
skills/cursor/
# Generated skills for review (make skills); keep directory via .gitkeep
skills/out/*
!skills/out/.gitkeep
